version: '2'
services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.builder
    entrypoint:
      - sh
      - '-c'
    environment: []
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock'
    network_mode: host
volumes: {}
